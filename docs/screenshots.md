# Screenshots

Do not commit invented product GIFs. After a real ingest, capture these from a running instance and attach them to the GitHub Release:

1. Landing (`/`)
2. Repository overview (health, languages, Full analysis status)
3. Explorer with a file from `examples/demo-ts/src`
4. Architecture graph with filters
5. Impact on `src/payments/charge.ts`
6. Ask answer with citations

Desktop and one mobile width are enough. Redact tokens.
